import React from 'react';
import Navigation from '../components/Navigation';

const Recipe = () => {
    return (
        <div>
            <Navigation/>
            <h1>Les recettes</h1>
        </div>
    );
};

export default Recipe;